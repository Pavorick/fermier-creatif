---
import { getCollection } from 'astro:content';
const posts = await getCollection('blog');
---

<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fermier Créatif | Autonomie & Innovation</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-stone-50 text-stone-900 font-sans">

    <section class="min-h-screen flex flex-col justify-center items-center text-center px-6 bg-white">
        <div class="mb-4 text-green-600 font-bold tracking-widest uppercase text-sm">Notre-Dame-de-l'Île-Perrot</div>
        <h1 class="text-6xl md:text-8xl font-black tracking-tighter mb-6">FERMIER<br/><span class="text-green-600">CRÉATIF</span></h1>
        <p class="text-xl text-stone-500 max-w-2xl italic">"De la science des sols à l'automatisation de la serre : documenter notre chemin vers l'autonomie."</p>
        <a href="#dossiers" class="mt-12 px-8 py-4 bg-stone-900 text-white font-bold rounded-full hover:bg-green-700 transition">Explorer les dossiers</a>
    </section>

    <section class="py-24 bg-stone-100 px-6">
        <div class="max-w-6xl mx-auto grid md:grid-cols-3 gap-12">
            <div>
                <h3 class="font-bold text-xl mb-4">La Science du Sol</h3>
                <p class="text-stone-600">Analyse des microorganismes, gestion des sols argileux et fertilisation naturelle.</p>
            </div>
            <div>
                <h3 class="font-bold text-xl mb-4">Innovation</h3>
                <p class="text-stone-600">Aquaponie (truites et tilapias), automatisation de serre et culture de champignons.</p>
            </div>
            <div>
                <h3 class="font-bold text-xl mb-4">Élevage Éthique</h3>
                <p class="text-stone-600">Des poules heureuses intégrées au cycle du compost et de la gélinette.</p>
            </div>
        </div>
    </section>

    <section id="dossiers" class="py-24 px-6 max-w-6xl mx-auto">
        <div class="flex justify-between items-end mb-12">
            <div>
                <h2 class="text-4xl font-bold">Le Blog du Fermier</h2>
                <p class="text-stone-500 mt-2">Nos fiches techniques et retours d'expérience.</p>
            </div>
            <div class="text-stone-400 font-mono text-sm">{posts.length} dossiers disponibles</div>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {posts.map(post => (
                <article class="group border-b border-stone-200 pb-8">
                    <a href={`/blog/${post.slug}`}>
                        <div class="aspect-video bg-stone-200 rounded-lg mb-4 overflow-hidden">
                            <div class="w-full h-full bg-green-900/10 group-hover:bg-green-900/20 transition-all duration-500"></div>
                        </div>
                        <h3 class="text-2xl font-bold group-hover:text-green-600 transition">{post.data.title}</h3>
                        <p class="text-stone-500 mt-2 line-clamp-2 italic">Découvrir ce dossier technique...</p>
                        <div class="mt-4 text-xs font-bold uppercase tracking-widest text-green-600 underline">Lire la suite</div>
                    </a>
                </article>
            ))}
        </div>
    </section>

    <footer class="py-12 border-t border-stone-200 text-center text-stone-400 text-xs uppercase tracking-widest">
        &copy; 2026 Fermier Créatif. Tous droits réservés.
    </footer>

</body>
</html>